{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "composer install && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "vendor/bin/heroku-php-apache2 public/ || php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}